/* See schema below.
    module.exports = {
        name: "name",
        aliases: ["al", "ia", "ses"],
        help: "help",
        usage: "usage",
        async execute(client, message, args) {
            code
        }
    }
 */
